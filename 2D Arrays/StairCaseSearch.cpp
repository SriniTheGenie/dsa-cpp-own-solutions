//for staircase search algorithm refer to Sudarsan Rajagopalan's repository


// In the worst case we are traversing One row and One column in staircase search, so we have effectively reduced the
// complexity from quadratic O(N^2) to O(N+M) <=> O(row + column)
